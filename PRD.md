# PRD — Obsidian GraphRAG 에이전트 (가칭 `naruto`)

| | |
|---|---|
| 문서 버전 | 0.4 (초안) |
| 작성일 | 2026-08-18 |
| 작성자 | 이동민 |
| 상태 | 검토 중 · Q1~Q4 확정, Q5~Q8 미해결 |
| 관련 과정 | 2026 오픈소스 AI 활용 인재 양성 풀스택 과정 / 트랙B (김정석 멘토) |

> **초안 주의.** 아래 수치와 범위는 근거 있는 출발점이지 확정값이 아닙니다.
> 1~3주차에 실제 노트를 쌓아보고, 4주차 베이스라인 측정 후 §7 지표를 재조정하는 것을 전제로 씁니다.

---

## 1. 배경 및 문제 정의

### 1.1 상황
학습·업무 과정에서 얻은 지식이 여러 곳에 흩어진다 — 브라우저 북마크, 캡처 이미지, 코드 주석, 기억. 시간이 지나면 **"분명 어딘가에서 봤는데"** 상태가 되고, 결국 같은 내용을 처음부터 다시 검색한다.

### 1.2 왜 단순 검색으로 부족한가
파일 이름·키워드 검색(Ctrl+F, `grep`)은 **정확한 단어를 이미 알고 있을 때만** 동작한다. 실제로 필요한 질문은 이런 형태다.

- "임베딩 모델 고를 때 내가 뭘 기준으로 삼았더라?" → 어느 노트에도 그 문장이 그대로 없음
- "Neo4j랑 pgvector 중에 왜 Neo4j로 갔지?" → 판단 근거가 3개 노트에 흩어져 있음
- "LangGraph 관련해서 내가 정리한 것 전부" → 노트 간 관계를 따라가야 함

앞의 둘은 **의미 기반 검색(벡터 RAG)** 이, 세 번째는 **노트 간 연결 구조(GraphRAG)** 가 필요하다. 이 프로젝트가 GraphRAG를 쓰는 이유가 여기 있다 — Obsidian의 `[[위키링크]]`는 이미 사람이 손으로 만들어둔 지식 그래프이고, 이걸 버리는 건 아깝다.

### 1.3 개인적 동기
- Obsidian을 써본 적이 없다. 이 프로젝트가 **지식 관리 습관을 들이는 강제 장치**를 겸한다.
- AI 직무 전환을 위한 포트폴리오가 필요하다.

---

## 2. 목표 (Goals)

| # | 목표 | 완료 판정 |
|---|---|---|
| G1 | 내 Obsidian 볼트 전체에 자연어로 질문하고 **출처가 달린** 답을 받는다 | 답변마다 근거 노트 파일명 + 해당 구절이 표시됨 |
| G2 | 벡터 검색만으로 못 푸는 **관계형 질문**을 GraphRAG로 처리한다 | §7 그래프 질문 세트에서 벡터 전용 대비 정답률 우위 확인 |
| G3 | 누구나 URL로 접속해 써보는 **공개 데모**를 배포한다 | Hugging Face Spaces에서 정상 동작 |
| G4 | 성능을 **숫자로** 증명한다 | RAGAS 평가 보고서 + 베이스라인 대비 개선 기록 |
| G5 | 코드와 문서를 GitHub에 공개한다 | README·PRD·평가 보고서 포함 |

### 2.1 비목표 (Non-Goals) — 이번 범위 밖

명시적으로 **안 한다.** 나중에 아쉬우면 v2에서 한다.

- ❌ **노트 편집·생성** — 읽기 전용. 에이전트가 볼트에 쓰기를 시작하면 데이터 정합성·백업 문제가 프로젝트를 삼킨다.
- ❌ **다중 사용자·인증** — 1인용. 계정 시스템 없음.
- ❌ **모바일 앱 / Obsidian 플러그인** — 웹 UI 하나만.
- ❌ **실시간 동기화** — 볼트 변경 반영은 수동 재색인(배치)으로 충분.
- ❌ **PDF·이미지 등 첨부파일 파싱** — 마크다운 본문만. (아이러니하게도 이 과정 PDF 자체가 텍스트 추출이 안 되는 이미지 PDF였다.)
- ❌ **모델 파인튜닝** — 공개 모델 조합·검증까지가 과정 취지.
- ❌ **로컬 GPU 추론 서버 운영** — 공개 모델은 호스팅 추론 API로 호출한다. 근거는 §6.1.
- ❌ **회사 업무 자료 투입** — §8 참조. 공개 배포이므로 절대 넣지 않는다.

---

## 3. 사용자 및 시나리오

### 3.1 주 사용자
**본인 (1인).** AI 직무 전환을 준비하는 현업 개발자. 부차적으로 데모 URL을 열어보는 **채용 담당자·멘토** — 이들은 내 볼트 내용을 모르므로, 예시 질문 버튼이 필요하다.

### 3.2 핵심 시나리오

**S1 — 기억 되살리기 (가장 빈번)**
> "4주차에 임베딩 모델 비교했던 거, 결론이 뭐였지?"

기대: 해당 노트 요약 + 원문 링크. 답을 모르면 **모른다고 말한다.**

**S2 — 흩어진 근거 모으기**
> "Neo4j를 선택한 이유를 정리해줘"

기대: 여러 노트에 흩어진 근거를 종합하고, 각 문장에 어느 노트에서 왔는지 표시.

**S3 — 관계 탐색 (GraphRAG 차별점)**
> "LangGraph 노트와 연결된 노트들을 따라가면서 전체 맥락을 설명해줘"

기대: `[[링크]]` 그래프를 N홉 순회해 관련 노트 묶음을 찾고 맥락을 구성. **벡터 검색만으로는 링크는 걸려 있지만 단어가 겹치지 않는 노트를 놓친다.**

**S4 — 빈 곳 찾기**
> "RAGAS에 대해 내가 정리해둔 게 있나?"

기대: 없으면 **"관련 노트 없음"** 이라고 답한다. 지어내지 않는다. (환각 방지가 가장 중요한 시나리오)

---

## 4. 지식 베이스 (KB) 계획

> ⚠️ **이 프로젝트 최대 리스크.** Obsidian을 써본 적이 없으므로 **볼트가 비어 있다.** RAG는 데이터가 없으면 평가조차 불가능하다. 1~3주차의 실질 과제는 코드가 아니라 **노트 축적**이다.

### 4.1 무엇을 넣는가

**본 과정의 학습 노트 자체를 지식 베이스로 삼는다.** 자기참조적이지만 여러 이유로 최선이다.

- 데이터가 **확실히 생긴다** — 14주간 매주 공부하므로 자동으로 쌓인다
- 내용을 **내가 100% 안다** — 답변이 맞는지 즉시 판단 가능, 평가셋 만들기 쉽다
- **공개해도 안전하다** — 회사 자료가 아니다
- 스토리가 된다 — "이 프로젝트를 만들며 쌓은 노트로 이 프로젝트를 검색한다"

포함 대상:
- 주차별 학습 정리 (RAG, GraphRAG, LangGraph, RAGAS, Docker …)
- 기술 선택의 **판단 근거와 기각한 대안** ← S2 시나리오의 연료
- 삽질 기록 (에러 → 원인 → 해결) ← 실제로 가장 자주 다시 찾게 되는 내용
- 읽은 논문·문서 요약

### 4.2 최소 수량 목표

| 시점 | 노트 수 | 용도 |
|---|---|---|
| 3주차 말 | **50개 이상** | 색인 파이프라인 개발 |
| 6주차 말 | 100개 이상 | GraphRAG 의미 있는 그래프 형성 |
| 11주차 말 | 150개 이상 | RAGAS 평가 신뢰도 확보 |

노트당 300~1500자. **노트당 `[[링크]]` 2개 이상**을 규칙으로 삼는다 — 링크가 없으면 그래프가 없고, 그래프가 없으면 GraphRAG가 벡터 RAG와 같아진다.

### 4.3 부족할 경우 (컨틴전시)
3주차에 50개가 안 되면 → 공개 기술 문서(LangChain·Neo4j 공식 문서 등)를 요약해 노트화하여 보충. 라이선스 확인 필수.

### 4.4 노트 작성 규칙 (확정)

**작성 언어는 한국어** (Q1 확정). 임베딩·LLM 선택은 여기에 종속된다 — §8.2 참조.

설계 원칙: **파이프라인이 실제로 소비하는 것만 규칙으로 만든다.** 규칙이 많을수록 노트 쓰기가 부담이 되고, 노트가 안 쌓이면 §4.2 목표를 못 채워 프로젝트 전체가 무너진다. 아래 5개가 전부다.

| # | 규칙 | 파이프라인에서의 용도 |
|---|---|---|
| **R1** | 파일명 = 제목. 한국어 명사구, 날짜·번호 접두사 없음 | `[[링크]]` 대상이자 **출처 표시(F5)에 그대로 노출됨**. 파일명이 지저분하면 인용이 지저분해진다 |
| **R2** | 프론트매터 3줄 고정 (`type`, `tags`, `created`) | `type`은 **Neo4j 노드 레이블**, `tags`는 필터·엣지가 된다 |
| **R3** | 본문을 `##` 소제목으로 구획 | **청크 분할 경계.** 고정 길이 분할보다 문맥 보존이 압도적으로 낫다 |
| **R4** | `[[링크]]` 2개 이상 | 그래프 엣지. **없으면 GraphRAG가 벡터 RAG와 같아진다** |
| **R5** | 첫 문단은 1~2문장 요약 | 노트의 첫 청크는 검색에 가장 자주 걸린다. 자기완결적 요약이 검색 정확도와 인용 품질을 동시에 올린다 |

#### 노트 타입 (R2의 `type`)

4개뿐이며, §3.2 시나리오와 1:1로 대응한다.

| type | 내용 | 대응 시나리오 |
|---|---|---|
| `concept` | 개념·기술 정리 (RAG란, Neo4j 인덱스 등) | S1, S4 |
| `log` | 삽질 기록: 증상 → 원인 → 해결 | S1 — **실무에서 가장 자주 다시 찾게 되는 유형** |
| `decision` | 선택한 것 + **기각한 대안과 그 이유** | S2 — 이 타입이 §3.2 S2의 유일한 연료다 |
| `summary` | 읽은 문서·논문 요약 | S4 |

> `decision` 노트를 의식적으로 쓸 것. 기술 선택의 근거는 당시엔 당연해 보여서 안 적게 되는데, **3개월 뒤 가장 아쉬운 게 바로 그것**이다. "왜 Neo4j였지?" 같은 질문에 답하려면 기각한 대안까지 적혀 있어야 한다.

#### 템플릿

````markdown
---
type: decision
tags: [graphrag, neo4j]
created: 2026-09-01
---

벡터 스토어와 별개로 그래프 DB가 필요해 Neo4j를 선택했다. pgvector 단독으로는
[[위키링크]] 관계 순회를 표현할 수 없다는 점이 결정적이었다.

## 배경
...

## 검토한 대안
- **pgvector 단독** — 운영은 단순하나 N홉 순회 불가
- **Neo4j** — Cypher로 그래프 순회가 자연스러움. 과정 지정 스택이기도 함

## 결정
Neo4j. 다만 [[Hugging Face Spaces 배포 제약]]에서 리소스 확인 필요.

## 남은 문제
무료 티어 메모리 한계 → [[Neo4j Aura]] 대안 검토
````

#### 하지 않을 것

**규칙을 안 만드는 것도 결정이다.** 아래는 의도적으로 배제한다.

- ❌ **폴더 분류 체계** — 전부 한 폴더에 평평하게. 폴더는 노트를 쓰기 전에 분류를 강요해서 글쓰기를 멈추게 한다. 태그와 링크로 충분하고, 파이프라인도 폴더를 쓰지 않는다
- ❌ **Templater 등 플러그인** — 1주차부터 도구 설정에 시간을 쓰지 않는다. Obsidian 기본 코어 템플릿 기능이면 족하다
- ❌ **MOC(목차 노트)** — 노트가 쌓인 뒤 자연스럽게 필요해지면 만든다. 미리 만들면 빈 껍데기가 된다
- ❌ **분량 규정** — 세 줄짜리 노트도 유효하다. 안 쓰는 것보다 낫다
- ❌ **태그 사전(taxonomy)** — 태그는 떠오르는 대로 붙이고, 6주차에 한 번 정리한다

---

## 5. 기능 요구사항

### P0 — 없으면 프로젝트가 성립 안 함

| ID | 기능 | 상세 |
|---|---|---|
| F1 | 볼트 색인 | 마크다운 파싱 → 청크 분할 → 임베딩 → 벡터 저장 |
| F2 | 링크 그래프 구축 | `[[위키링크]]`·태그 추출 → Neo4j 노드/엣지 |
| F3 | 하이브리드 검색 | 벡터 유사도 + 그래프 순회 결과 병합 |
| F4 | 답변 생성 | 검색 결과 기반 LLM 답변 |
| F5 | **출처 표시** | 답변에 근거 노트명 + 인용 구절. **누락 시 F4는 무가치** |
| F6 | **모름 처리** | 근거 부족 시 추측 대신 "관련 노트 없음" |
| F7 | 채팅 UI | 질문 입력 → 답변 + 출처 표시 |
| F8 | 재색인 | 볼트 변경 후 수동 트리거 |

### P1 — 있으면 데모 품질이 확 오름

| ID | 기능 | 상세 |
|---|---|---|
| F9 | 예시 질문 버튼 | 방문자가 뭘 물어야 할지 모름. 데모 성패를 가름 |
| F10 | 검색 과정 표시 | 어떤 노트를 어떤 경로로 찾았는지 노출 (GraphRAG 차별점 시연) |
| F11 | 대화 맥락 유지 | 후속 질문("그럼 그건 왜?") 처리 |
| F12 | 그래프 시각화 | 참조된 노트 간 연결 표시 |

### P2 — 시간 남으면
F13 답변 피드백(👍/👎) · F14 답변 스트리밍 · F15 검색 파라미터 조절 UI

---

## 6. 시스템 개요

```
Obsidian 볼트 (.md)
      │
      ▼
  [색인 파이프라인]  ── 파싱 · 청크 분할 · 임베딩
      │
      ├──────────────┐
      ▼              ▼
 벡터 스토어      Neo4j (노트 그래프)
      │              │
      └──────┬───────┘
             ▼
     [LangGraph 에이전트]  ── 질문 분석 → 검색 → 근거 검증 → 답변
             │
             ▼
      FastAPI  →  웹 UI  (Docker → HF Spaces)
                          ▲
                    [RAGAS 평가]
```

**스택은 과정에서 지정된 것을 따른다** — Python(FastAPI, LangChain, LangGraph), Obsidian, LLM-Wiki, Neo4j, Docker, Hugging Face Spaces, RAGAS. 상세 설계는 별도 문서로 분리한다. (PRD는 What/Why, 설계서는 How)

### 6.1 LLM 구성 (확정)

Q3이 오래 안 풀린 이유는 "공개 모델 vs 상용 API"를 **하나의 선택**으로 놨기 때문이다. 실제로는 LLM이 앉는 자리가 셋이고, 각 자리의 최적해가 다르다.

| 역할 | 결정 | 근거 |
|---|---|---|
| **서빙 LLM** (F4 답변 생성) | **가중치 공개 모델을 호스팅 추론 API로 호출.** 후보 2~3종(Llama·Qwen 계열 등 한국어 가능 모델)을 4주차에 비교해 확정. 후보별로 **엔드포인트·가격·rate limit·웜 p50/p95 지연**을 함께 기록한다 | 과정 취지(공개 모델)와 §7.3 응답 목표를 동시에 만족하는 현실적인 조합 |
| **judge LLM** (RAGAS 채점) | **상용 API(Claude), 모델 버전 고정** | 심판은 제품이 아니라 **계측 장비**다. 피심판(오픈 모델)과 심판이 다른 편이 자기편파도 막는다. 버전을 고정해야 주차 간 점수를 비교할 수 있다 |
| **비교 실험** (§7.4 산출물) | 동일 평가셋으로 **오픈 호스팅 모델 vs 상용 API** 2종 비교 → 품질·지연·비용 표 | Q3의 "비교 실험 자체를 산출물로"를 실행 가능한 형태로 확정한 것 |

#### 왜 로컬 실행을 서빙 경로에서 배제하나

**"공개 모델"과 "로컬 실행"은 별개의 축이다.** 가중치가 공개된 모델을 호스팅 API로 부르는 것도 공개 모델 사용이다. 그리고 로컬 실행은 이 프로젝트에서 물리적으로 불가능하다 — 추정이 아니라 확인한 사실이다.

- 개발 머신: Intel UHD 770 내장 그래픽 / RAM 16GB / 외장 GPU 없음 → 양자화 7~8B도 **CPU 추론**, 답변 1건에 수십 초
- 배포 대상인 **HF Spaces 무료 티어도 CPU** — 같은 제약이 그대로 재현됨
- §7.3의 "응답 10초 이내"와 정면 충돌하고, 100문항대 RAGAS 평가를 반복하면 측정 자체가 몇 시간짜리 작업이 된다

로컬 추론 서버 운영은 §2.1 비목표로 명시한다.

#### 폴백 인터페이스

서빙 LLM은 LangChain의 chat model 추상화 뒤에 두고 **환경변수로 교체 가능**하게 만든다. 오픈 모델의 한국어 품질이 목표에 못 미치면 데모는 상용 API로 방어하되, **보고서의 주 수치는 오픈 모델 기준으로 보고**한다. 데모 성패와 과정 취지를 동시에 지키려면 이 구조여야 한다.

### 6.2 임베딩 구성 (확정)

Q3과 결론이 갈린다. 임베딩 모델은 소형이라 CPU에서 돌려도 §7.3 재색인 목표를 만족한다 — **Q3에서 로컬 실행을 배제한 근거(지연)가 여기엔 적용되지 않는다.**

| 항목 | 결정 |
|---|---|
| 실행 방식 | **자체 호스팅.** 임베딩 API는 쓰지 않는다 |
| 후보 상한 | **파라미터 ≤ 0.6B** 그리고 **출력 차원 ≤ 1024.** 둘은 독립된 축이다 — 파라미터는 추론 메모리·속도를, 차원은 인덱스 크기·검색 비용을 좌우한다. 384·768·1024차원을 **모두** 후보로 허용한다. 8B급 임베딩 모델은 FP16 가중치만 ≈16GB로, 배포 환경(CPU 2코어·16GB를 Neo4j·앱과 공유)에 산술적으로 들어가지 않는다 |
| 선정 기준 (우선순위) | ① 한국어 검색 품질 ② 가중치 공개 + 상업적 사용 가능 라이선스 ③ 청크 토큰 분포를 덮는 max sequence length ④ 배포 환경 메모리 여유 |
| 선정 시점 | 4주차. 단 **1~2주차에 CPU 2코어 조건에서 RSS·초당 청크 수·전체 재색인 시간을 먼저 측정**해 후보를 걸러낸다 |

#### 선정 방법 (4주차)

- 공개 리더보드(MTEB 한국어 등)는 **후보 선별용으로만** 쓴다. 최종 판단은 내 볼트로 만든 retrieval 평가셋 기준이다. 남의 벤치마크 1위가 내 노트에서 1위일 이유가 없다.
- 평가셋을 **개발용 / hold-out으로 분리**한다. 분리 단위는 **노트**다 — 청크 단위로 나누면 같은 노트의 인접 청크가 양쪽에 들어가 누수가 생긴다.
- 모델 선택은 개발용으로만 한다. hold-out은 선택이 끝난 뒤 **한 번만** 돌려 보고서에 싣고, **그 결과를 보고 모델을 다시 고르지 않는다.** 다시 고르면 그건 hold-out이 아니다.
- 측정 정의: `k=5` 고정, 정답은 **청크 단위** 판정, 복수 정답 허용. 지표는 Recall@k·MRR.
- **max sequence length는 모델 스펙이 아니라 내 청크 토큰 분포로 판단한다.** 후보별 토크나이저로 (prefix·instruction을 붙인) **최종 입력 길이**의 p50/p95/max를 재고, 합격선은 **p95 무절단 · 전체 truncation ≤ 1%**. 모델 최대 길이는 청크 목표 길이가 아니며, 긴 입력은 CPU 지연과 메모리만 늘린다.

#### 동결 대상 (재현성)

모델명만 고정해선 검색 결과가 재현되지 않는다. 아래를 고정하고 **인덱스 메타데이터에 함께 저장**한다.

- 모델 저장소 revision(commit SHA), tokenizer revision
- pooling 방식, query/passage prefix·instruction — **E5 계열은 prefix가 검색 품질에 직결된다**
- max token·truncation 설정, dtype, 정규화 여부, 거리 함수
- 청킹 규칙 버전, 색인 시점의 볼트 커밋 SHA
- `transformers`·`sentence-transformers` 등 임베딩 라이브러리 버전
- 인덱스 구현·파라미터 (Q8 확정 후 추가)

목표는 비트 단위로 같은 벡터가 아니라 **"평가 지표 허용 오차 내 재현"** 이다. CPU·라이브러리 차이로 부동소수점 결과는 어차피 미세하게 달라진다. 변경 시 **전체 재색인 + 베이스라인 재측정** — judge 고정(§6.1)과 같은 논리다.

#### 데이터 흐름 — 볼트 내용이 외부로 나가는 지점

자체 호스팅이므로 **색인 단계에서는 볼트 본문이 외부로 나가지 않는다.** 그러나 이후 두 지점에서 나간다.

1. **서빙** — 사용자 질문 + 검색된 청크가 서빙 LLM API로 전송된다
2. **평가** — 질문·검색된 청크·생성된 답변·평가셋 정답이 RAGAS judge API로 전송된다

따라서 §8.1의 "회사·민감 정보 금지"는 임베딩이 로컬인 것과 **무관하게 그대로 유효**하다. 각 API 제공자의 로그 보존 정책은 6주차 배포 전에 확인한다. 공개 데모가 질문을 통해 노트 내용을 끌어낼 수 있다는 점도 전제한다 (Q5).

---

## 7. 성공 지표

### 7.1 품질 (RAGAS)

**베이스라인을 먼저 측정하고 목표를 확정한다.** 아래는 잠정 기준선이다.

| 지표 | 의미 | 잠정 목표 | 우선순위 |
|---|---|---|---|
| **Faithfulness** | 답변이 검색된 근거에 충실한가 (= 환각 안 하는가) | **≥ 0.85** | 최우선 |
| Answer Relevancy | 질문에 맞는 답인가 | ≥ 0.80 | 높음 |
| Context Precision | 검색된 문서 중 실제로 쓸모 있던 비율 | ≥ 0.70 | 중간 |
| Context Recall | 필요한 문서를 다 찾았는가 | ≥ 0.75 | 중간 |

> **점수 재현 조건:** judge LLM과 그 버전을 고정한다(§6.1). 심판이 바뀌면 주차 간 점수 비교가 무의미해지므로, judge 교체는 베이스라인 재측정과 함께만 한다.

> Faithfulness를 최우선에 두는 이유: **틀린 답을 그럴듯하게 하는 것이 못 찾는 것보다 나쁘다.** 내 노트를 검색했는데 없는 내용을 지어내면 도구 자체를 신뢰할 수 없게 된다.

### 7.2 GraphRAG 효과 검증 (G2)

**이게 없으면 Neo4j를 쓸 이유를 증명 못 한다.**

- 관계형 질문 **20개 이상**의 평가셋을 별도로 만든다 (S3 유형)
- 벡터 전용 vs 벡터+그래프 를 **동일 질문으로 비교** 측정
- 목표: 해당 세트에서 Context Recall **10%p 이상** 우위
- 우위가 없으면 → 그래프 순회 전략을 고치거나, **"효과 없었음"을 근거와 함께 보고서에 쓴다** (음성 결과도 결과다)

### 7.3 시스템
- 응답 시간 **웜 요청 p95 10초 이내** (콜드스타트 제외, 스트리밍 시 첫 토큰 3초)
  - 측정 조건: 질문 1문장 · 검색 `k=5` · 출력 300토큰 이내 · 동시성 1 · 워밍업 3회 후 30회 이상 · HTTP 요청 시작부터 최종 토큰까지
- 150개 노트 전체 재색인 10분 이내
- **콜드스타트 후 정상 동작.** 무료·저가 티어는 미사용 시 sleep한다 — 무중단 가동은 이 프로젝트의 목적이 아니다(§2.1). G3의 요구는 "URL을 주면 동작한다"이다

### 7.4 산출물 (포트폴리오 3종)
- [ ] 배포된 서비스 URL
- [ ] PRD + 설계 문서 (본 문서 포함)
- [ ] RAGAS 평가 보고서 (베이스라인 → 개선 과정 기록 + §6.1 LLM 비교 실험 표)

---

## 8. 제약 및 리스크

### 8.1 제약
- **공개 배포** — 볼트 내용이 인터넷에 노출된다. 회사·개인 민감 정보 절대 금지
- **HF Spaces 무료 티어** — CPU·메모리·디스크 제한. Neo4j를 같은 컨테이너에 올릴 수 있는지 확인 필요
- **Claude Code는 보조 도구** — 과정 규정. 최종 평가는 본인의 코드 이해도. **이해 못 한 코드는 커밋하지 않는다**
- **주 2회 세션 + 본업 병행** — 실질 가용 시간이 프로젝트 범위의 상한

### 8.2 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| **노트가 안 쌓임** | 치명적 — 평가 불가 | 주간 최소 노트 수를 스스로 강제. 3주차 체크포인트에서 §4.3 발동 |
| Neo4j를 HF Spaces에 못 올림 | 높음 | **6주차까지 배포 가능성 선검증.** 대안: Neo4j Aura 무료 티어 |
| **HF Spaces 무료로는 Docker Space를 만들 수 없음** | 높음 — G3 전제가 깨짐 | 공식 문서 확인(2026-08-19): Gradio·Docker Space 생성은 **개인 PRO(월 $9) 필요**, 무료는 static Space 또는 ZeroGPU 기반 Gradio 2개뿐이며 **ZeroGPU는 Neo4j를 띄우는 일반 컨테이너가 아니다.** 6주차 배포 스파이크의 합격 조건(재시작 복구·메모리·공개 URL·월 비용)을 먼저 정하고, 실패 시 **PRO 결제를 기본 fallback**으로 둔다. 배포는 6주차 이후에만 필요하므로 그때까지 지출 0 |
| 재시작 시 로컬 상태 소실 | 중간 | Spaces 디스크는 비영구(50GB, not persistent)이므로 sleep·재빌드·재시작 때마다 벡터·그래프가 사라질 수 있다. **색인 산출물을 저장소에 커밋하는 방식은 쓰지 않는다** — G5가 공개 저장소이므로 노트 본문·파생물이 노출된다. 기본안은 **외부 영속 저장소(Neo4j Aura 등)** |
| 링크가 부족해 그래프가 빈약 | 높음 — G2 무의미 | 노트당 링크 2개 규칙. 6주차에 그래프 밀도 점검 |
| LLM API 비용 | 중간 | 평가 반복 시 비용 누적. judge 호출 결과 캐싱 + 평가셋 크기 관리. 서빙은 오픈 모델이라 비용 대부분이 judge 쪽에서 발생 |
| **오픈 모델의 한국어 품질 미달** | 높음 — 서빙 경로가 흔들림 | 4주차 비교 실험이 조기 경보 역할. 미달 시 §6.1 폴백 인터페이스로 데모는 상용 API 방어, 보고서는 오픈 모델 수치를 그대로 기록 |
| 호스팅 API 무료 티어 제한·종료 | 중간 | rate limit·서비스 종료로 데모가 멈출 수 있음. **프로바이더 2곳의 키를 미리 확보**하고 환경변수로 전환 가능하게 |
| 한국어 임베딩 품질 | 중간 | Q1 확정으로 **한국어 지원이 임베딩 모델의 필수 조건**이 됨. 4주차에 다국어 모델 2~3종 비교 후 선택 |
| 한국어 청크 크기 오산정 | 중간 | 영어 기준 청크 가이드(글자 수)를 그대로 쓰면 안 됨. **실제 토크나이저로 토큰 수를 측정**해 정할 것 |
| 범위 확장 | 중간 | §2.1 비목표를 방패로 사용 |

---

## 9. 미해결 질문

**답을 정해야 다음 단계가 열리는 것들.** 대부분 나중에 결정해도 되지만, Q1·Q2는 1주차에 필요하다.

| # | 질문 | 필요 시점 | 현재 생각 |
|---|---|---|---|
| ~~Q1~~ | ~~노트 작성 언어~~ | ~~1주차~~ | ✅ **한국어로 확정** (2026-08-18) |
| ~~Q2~~ | ~~노트 작성 규칙 범위~~ | ~~1주차~~ | ✅ **확정 — §4.4** (2026-08-18) |
| ~~Q3~~ | ~~LLM은 어떤 걸 쓰나?~~ | ~~4주차~~ | ✅ **확정 — §6.1** (2026-08-19). 서빙=오픈 모델 호스팅 API, judge=상용 API 고정, 둘의 비교를 산출물로. 구체 모델명은 4주차 비교로 |
| ~~Q4~~ | ~~임베딩 모델?~~ | ~~4주차~~ | ✅ **확정 — §6.2** (2026-08-19). 자체 호스팅 오픈 모델, 파라미터 ≤0.6B · 차원 ≤1024. 구체 모델명은 4주차 비교로 |
| Q5 | 데모용 볼트를 별도로 둘까? | **6주차** (12주차에서 앞당김) | 공개 배포 시 개인 노트 노출 범위 재검토 필요. 배포 시점에 필요하고, 12주차엔 노트가 이미 쌓여 분리 비용이 커진다 |
| Q6 | 서비스 이름? | 12주차 | 현재 저장소명 `naruto`는 임시 |
| Q7 | LLM-Wiki를 정확히 어디에 쓰나? | 멘토 확인 | 과정 스택에 명시돼 있으나 역할 불분명 |
| **Q8** | 벡터 스토어 구성: Neo4j 벡터 인덱스 단일 vs 별도 벡터 DB | 6주차 | 배포 선검증과 함께 결정. 차원·거리 함수는 §6.2에 종속되고, 인덱스 파라미터는 확정 후 §6.2 동결 대상에 추가 |

---

## 10. 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| 0.4 | 2026-08-19 | Q4(임베딩 구성) 확정 — §6.2 신설. Codex 외부 리뷰 반영: 응답 지연을 웜 p95 + 측정 조건으로 교체하고 무중단 목표 삭제, HF Spaces 무료 Docker 불가·재시작 상태 소실 리스크 2건 추가, Q8 신설, Q5를 6주차로 앞당김 |
| 0.3 | 2026-08-19 | Q3(LLM 구성) 확정. §6.1 신설, 로컬 추론 서버를 비목표로 추가, 오픈 모델 품질·호스팅 API 제한 리스크 2건 추가, judge 버전 고정 조건 명시 |
| 0.2 | 2026-08-18 | Q1(한국어)·Q2(노트 작성 규칙) 확정. §4.4 신설, 한국어 청크 크기 리스크 추가 |
| 0.1 | 2026-08-18 | 최초 초안 |
